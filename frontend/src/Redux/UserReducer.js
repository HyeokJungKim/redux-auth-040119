let initialState = {
  id: "",
  username: "",
  money: 0,
  snacks: [],
  token: ""
}

const userReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
}

export default userReducer
